type vendor_per_mgr, domain;
type vendor_per_mgr_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(vendor_per_mgr)

binder_use(vendor_per_mgr)
binder_service(vendor_per_mgr)

allow vendor_per_mgr vendor_per_mgr_service:service_manager { add find };
